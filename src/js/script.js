document.addEventListener('contextmenu', (event) => event.preventDefault());

/* const customAlert = document.getElementById('alert');

// Função para exibir a mensagem
function showCustomAlert() {
  customAlert.style.display = 'block'; // Mostra o alerta
  setTimeout(() => {
    customAlert.style.display = 'none'; // Oculta o alerta após 2 segundos
  }, 2000);
}

// Bloqueio de atalhos
document.addEventListener('keydown', (event) => {
  if (
    event.ctrlKey ||
    event.shiftKey ||
    event.altKey ||
    event.key.length === 1
  ) {
    event.preventDefault(); // Impede o comportamento padrão
    event.stopPropagation(); // Para a propagação do evento
    showCustomAlert(); // Mostra a mensagem
  }
});
 */
